run the app as root folder 

go run /cmd/apis/main.go

use this command to use this outstanding tool

curl -X POST http://localhost:8080/build \
  -H "Content-Type: application/json" \
  -d '{"feature": "feature2"}'

export these envs in the code

export HARBOR_USER=admin
export HARBOR_PASSWORD=Qbs@12345#


///Extras


